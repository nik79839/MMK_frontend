export type calculationType = {
    id: string
    name: string
    calculationEnd: string
    sechName: string
    progress: number
    pathToRegim: string
    percentLoad: number
    percentForWorsening: number
    description?: string
};